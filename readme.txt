import the library needed first
type in the following lines to install the library

	pip install matplotlib
	pip install Pillow
	python -m pip install numpy


a)how to run it?
    run command: 
        python -u main.py -input path1 -texton-size size1 -output-size size2 -output path2

b)Patch based approach algorithm

c)'Efros and Freeman 2001' is used.